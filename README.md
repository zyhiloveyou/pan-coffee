# 潘咖啡 · 移动端主页

一个为手机浏览优先设计的咖啡馆单页主页。日式侘寂风格（灰褐 + 纸感），**纯静态、零外部依赖**（无 CDN、无字体、无 JS 库），放在任何静态托管或本地打开都能跑，断网也正常显示。

```
pan-coffee/
├── index.html          页面结构（含 SEO / 结构化数据）
├── css/style.css       全部样式（移动优先，4 个断点）
├── js/main.js          交互（抽屉菜单、分类切换、滚动显隐、复制地址）
├── img/                logo 与图标（由你的原图生成，见「替换 logo」）
└── preview/            渲染预览图（非页面必需，可删）
```

## 本地预览

```bash
cd pan-coffee
python3 -m http.server 8080        # 然后手机浏览器访问 http://<电脑局域网IP>:8080
```

直接双击 `index.html` 也能看，但**用真机 + 局域网地址**才能看到固定底栏、安全区（刘海屏）等移动端效果。

预览图：`preview/mobile-hero.png`（首屏）、`preview/mobile-390.png`（全页）、`preview/desktop-1280.png`（桌面）。

## 需要你替换的内容

| 位置 | 行号 | 现在的内容 | 说明 |
| --- | --- | --- | --- |
| 电话 | `index.html:428`、`505`、`511` | `138 0000 0000` / `tel:13800000000` | 三处都要改，`tel:` 里写纯数字 |
| 微信号 | `index.html:519` | `data-wechat="pancoffee_sy"` | 底栏「微信预约」点按复制此号 |
| 营业时间 | `index.html:70`、`424`、`505` | 每日 09:00 – 21:00 | 三处 |
| ~~菜单与价格~~ | `index.html:202-266` | **已临时隐藏**（整段被注释） | 见「临时隐藏的内容」 |
| 页面说明文字 | `index.html:424`、`428` | 带「（示例…）」提示 | 换成真实信息后请删掉这两个 `<em>` |
| 照片 | `index.html:472-495` | 6 个 CSS 生成的占位色块 | 见下节 |
| 合作豆单 | `index.html:268-408` | 已按你的 docx 录入真实豆单与价格 | 见上节 |
| logo | `index.html:45`、`502` | 顶栏与页脚各一处 | 见「替换 logo」 |

店名、品牌名、地址、关于文案已按你给的信息填好（潘咖啡 / 辽宁省沈阳市大东区东塔民航小区 43 号楼）。

### 换成真实照片

把每张 `<figure class="shot">` 里的 `<div class="shot__ph shot__ph--N">` 换成 `<img>` 即可，样式不用动：

```html
<figure class="shot shot--tall">
  <img src="img/bar.jpg" alt="吧台与手冲壶" loading="lazy" decoding="async">
  <figcaption>吧台与手冲壶</figcaption>
</figure>
```

- 建议宽度 800px 左右、压缩到 150KB 以内；`.shot--tall` 是竖图位（高宽比约 3:2 竖版），`.shot--wide` 是整行横图。
- 说明文字是浅色压深色遮罩，换任何照片都清晰；想改成浅底深字，改 `.shot figcaption` 即可。

### 替换 logo

logo 由你的原图（`img/logo-original.jpg`，888×888 白底 JPG）加工而来，页面里用到两处：顶栏 30px 高、页脚 46px 高。

| 文件 | 尺寸 | 用途 |
| --- | --- | --- |
| `img/logo.png` | 160×128 | 顶栏与页脚显示（白底已转成**透明通道**） |
| `img/favicon-64.png` | 64×64 | 浏览器标签页图标（纸色底 + logo 居中） |
| `img/apple-touch-icon.png` | 180×180 | iOS「添加到主屏幕」图标 |
| `img/logo-original.jpg` | 888×888 | 原始文件存档，方便以后重新裁切 |

加工做了三件事：裁掉四周不均匀的白边（原图内容只占 730×582，且左上留白 112/176、右下只有 31/129）、把白底按覆盖度转成 alpha（顺带把 JPEG 的白区噪点阈值到 5% 以下，避免出现灰雾）、缩放到显示所需的 2–3 倍分辨率。

换成别的 logo 时：

- **有透明底 PNG**：直接覆盖 `img/logo.png` 即可，CSS 是 `height: 30px; width: auto`，任意长宽比都能自适应，不会变形。
- **仍是白底图**：别直接放上去（白色方块会压在纸色背景上很明显）。用同样的办法处理：把原图放到项目根目录后，我可以重新跑一遍生成脚本；或自己用支持「白色转透明」的工具导一次。
- 图标（favicon / 主屏图标）需要正方形：非正方形的 logo 会在 64×64、180×180 画布上居中留边，不会裁切。
- 页脚的 logo 在 `index.html` 的 `<footer>` 第一行，不想要删掉那一行 `<img class="footer__logo" …>` 即可。

## 页面包含的区块

首屏（店名 + 看豆单/到店导航）→ 壹 关于潘咖啡 → 贰 招牌推荐（横向滑动卡片）→ 叁 潘咖啡合作豆单（3 组 17 项，整袋价）→ 肆 到店信息（地址/营业/电话/地图按钮）→ 伍 店内一角（相册）→ 页脚，外加**固定底部操作栏**：打电话 / 导航到店 / 微信预约。

> 「菜单与价格」目前**临时隐藏**，见文末「临时隐藏的内容」。

## 合作豆单（第肆节，`#beans`）

内容来自你的 `潘咖啡合作豆单.docx`，已录入 17 项，按文档的三组分列，价格是**整袋价**：

| 分组 | 规格 | 项数 | 价格区间 |
| --- | --- | --- | --- |
| 拼配系列 | 454g 装 | 4 | ¥70 – ¥180 |
| SOE 系列 | 454g 装 | 2 | ¥100 |
| 单品豆系列 | 200g 装 | 11 | ¥60 – ¥140 |

排版上把文档里的「产地 处理法 名称」拆成了**名称 + 小标签**（例如「朵望丘 · 埃塞 · 72小时厌氧日晒」），风味描述单独一行，价格右对齐，方便在手机上快速扫读。分组标题右侧标注了规格单位，所以单品价格不再重复写「/454g」。

改动价格或增删豆子：直接改 `index.html` 里 `#beans` 区块的 `<li class="item">` 即可，结构是 `<h4>名称 <i>标签</i></h4>` + `<p>风味</p>` + `<p class="item__price">¥价格</p>`。

> 两处已与店主核对：
> 1. 拼配2号的「哥伦比他」是笔误，**已改为「哥伦比亚」**。
> 2. 拼配1号的「埃塞日晒、埃塞日晒、埃塞水洗」**重复是有意的**（同一款豆用两份），按原文保留。

`.docx` 已加入 `.gitignore`，不会提交到公开仓库（仍是本地原始资料）。想一起存档就 `git add -f 潘咖啡合作豆单.docx`。

## 移动端做了哪些针对性处理

- **固定底栏**：下滑时自动让出阅读空间，上滑或回到顶部立刻出现；底部预留 `env(safe-area-inset-bottom)` 适配 iPhone 手势条。
- **抽屉菜单**：点按展开，背景滚动用 `position:fixed` 锁死（iOS Safari 的 `overflow:hidden` 锁不住，已规避），关闭时精确回到原滚动位置；支持 Esc 关闭与 Tab 焦点循环。
- **菜单分类**：标签栏横向可滑、吸顶在顶栏下方，切换时把选中标签滚到中间。
- **模态安全**：`100svh` 避免地址栏伸缩跳动；矮屏手机（如 iPhone SE，568px 高）有专门压缩规则，保证标题与按钮都在第一屏内。
- **无 JS 也能读**：所有内容都在 HTML 里，JS 只做增强；脚本失效时相册、菜单、正文照常显示。
- **尊重系统设置**：`prefers-reduced-motion` 下关闭全部动画并让内容立即可见。

## 已实测验证的项

用无头 Chrome 以真实设备仿真（含 meta viewport、触摸、DPR）在 **320×568 / 390×844 / 768×1024 / 1280×900** 四种视口下测量：

- 无横向溢出（`scrollWidth == clientWidth`），越界元素 0；横向滑动区（招牌卡片、分类标签）为有意设计。
- 文字对比度 **28/28 通过 WCAG AA**（正文/小字 ≥ 4.5:1，灰褐 `#635c50`、苔绿 `#575d4b`、柿色 `#8a4a26` 均已按对比度重新取值）；按钮与标签描边 ≥ 3.4:1（WCAG 1.4.11）。
- 触摸目标：所有按钮/链接 ≥ 24px（WCAG 2.5.8），主要控件 ≥ 44px。
- 交互回归：抽屉开合 / Esc 关闭 / 滚动锁与位置还原 / 锚点跳转后正确落位 / 分类切换 aria 状态 / 底栏随滚动显隐 —— 全部通过。
- 无控制台报错、无失败请求、**零外部网络请求**（可离线、可内网）。
- HTML 标签闭合、id 唯一性、JS 语法、CSS 括号与自定义属性引用均校验通过。

## 部署

任何静态托管都能直接上传（对象存储、Nginx、Vercel/Netlify、GitHub Pages 均可），无需构建。上传 `index.html`、`css/`、`js/`（`preview/` 可不上传）。

几点建议：

- 微信内打开时，底栏「微信预约」复制的是微信号，客人长按粘贴搜索即可，比放二维码更省事；想放二维码可在页脚加一张图片。
- 地图按钮目前直接调起高德/Apple 地图**搜索地址**，不需要 API key。若想内嵌地图，再申请高德或百度地图 JS API 并填入坐标。
- 若要「添加到主屏」，可补一个 `manifest.json` + 图标（当前未包含，避免引入多余请求）。

## 临时隐藏的内容

### 菜单与价格（`#menu`）

因价格调整临时下线，做法是把整个区块用 HTML 注释包起来（见 `index.html:202-266`），**内容仍完整保留在文件里，随时可恢复**。隐藏期间同步做了四件事，恢复时要一并改回：

| # | 隐藏时改动 | 恢复动作 |
| --- | --- | --- |
| 1 | 区块被注释包裹 | 删掉 `<!-- ▸▸ 菜单与价格：临时隐藏 ▸▸` 那一行与末尾 `▸▸ …临时隐藏结束 ▸▸ -->` 那一行 |
| 2 | 豆单/到店/相册序号顺延为 叁/肆/伍 | 改回 肆/伍/陆（菜单自身仍是「叁」） |
| 3 | 首屏主按钮改为「看豆单」→ `#beans` | 改回 `href="#menu"`、文字「看菜单」（`index.html:100`） |
| 4 | 抽屉目录删掉「叁 菜单与价格」 | 加回该项，后面三项序号顺延回 肆/伍/陆 |

也可以直接回退这一次提交：`git revert <commit>`。

> 提示：HTML 注释里不能再出现注释标记，所以区块自带的 `<!-- 菜单 -->` 那行会在恢复时一并补回（不影响显示）。
